# Copied to .env by install.sh - shared settings used by every service
TZ=America/New_York
PUID=1000
PGID=1000

# Set automatically by install.sh if you enable the Traefik reverse proxy.
# Leave blank if you're accessing services via http://<server-ip>:<port>.
DOMAIN=
